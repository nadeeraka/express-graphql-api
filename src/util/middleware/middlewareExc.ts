// //middleware exc
// import express from 'express'

// const middlewareExc = async (arr:string[]):Promise<void>=>{

//     for (let i = 0; i < arr.length; i++) {
//         const element = await  `${arr[i]}`;
        
//     }

// }